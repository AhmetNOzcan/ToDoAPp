#!/bin/sh
# Blocks `git push` if `flutter analyze` reports errors.
# bloc_lint rules run as part of `flutter analyze` via each package's
# analysis_options.yaml `include: package:bloc_lint/recommended.yaml`.
# Bypass in emergencies with: git push --no-verify
set -e

echo "==> flutter analyze (workspace)"
flutter analyze
