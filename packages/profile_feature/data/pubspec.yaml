name: profile_feature_data
description: Profile feature — data layer (database, datasources, models, repository impls).
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  profile_feature_domain: 0.0.1
  sqflite: ^2.4.2
  path: ^1.9.0
  injectable: ^2.5.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
  injectable_generator: ^2.6.2
  build_runner: ^2.4.13

flutter:
