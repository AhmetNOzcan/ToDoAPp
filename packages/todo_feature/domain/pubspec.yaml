name: todo_feature_domain
description: Todo feature — domain layer (entities, repository contracts, use cases).
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  equatable: ^2.0.7

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
  mocktail: ^1.0.4
